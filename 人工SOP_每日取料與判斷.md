# SPY 當沖每日複盤 — 人工操作 SOP（取料與情境判斷）

> **對象**：每天早上負責做帳的同仁。不需要懂程式。
> 多數日子完全不需要 AI；**只有「拿不到 CSV、只有截圖」時才需要**（第 7 節）。
> **這份管什麼**：今天的資料**從哪裡來**、遇到各種狀況**怎麼判斷**。
> **拿到 CSV 之後怎麼跑**：雙擊 `每日一鍵複盤.bat` 就好，細節見《操作手冊》。
> **最後更新**：2026-07-27。

---

## 0. 三條鐵則（先背起來）

1. **看到 🔴 就停手**，把整個畫面截圖交給負責人。系統擋下來是**保護**，不是壞掉。
2. **絕對不要為了「把流程跑完」去改任何數字。** 寧可今天不做、明天補做。
   2026 年 7 月那幾次錯帳，全部是從「猜一下應該是這樣」開始的。
3. **不確定就問**。這份 SOP 沒寫到的狀況＝不在你的權限範圍，交負責人。

---

## 1. 每天第一件事：今天到底要不要做？

台北早上做的是**前一個美東交易日**的帳（美東日 ＝ 台北時間 − 12 小時）。

| 今天（台北）是 | 要處理的美東交易日 | 做不做 |
|---|---|---|
| 週二 ～ 週六 早上 | 前一天（美東 週一～週五） | ✅ 要做 |
| **週日、週一 早上** | 美股週六/週日休市 | ❌ 不用做 |
| 美股國定假日的隔天早上 | 休市 | ❌ 不用做 |

> **老闆當天沒下單**也是正常的：對帳單裡只有 `BAL` 行、沒有任何 `TRD` 行。
> 這種情況引擎會說找不到交易 → **不用做帳**，回報一句「當日無交易」即可。

---

## 2. 一頁決策樹

> 貼牆用的圖檔：**`決策樹.png`**（同一個資料夾，可直接列印）。

```
開始（台北早上）
  │
  │  ① 打開 Gmail：shuye1668@gmail.com →「草稿」匣
  │     找主旨 [SPY-DayTrade-Autosend] YYYY-MM-DD
  │     （日期通常＝今天的台北日期；不確定就取最新那封）
  ▼
◆ 有找到草稿嗎？
  │
  ├─【有】→ ◆ ② 內文有 ===== CSV CONTENT START / END ===== 嗎？
  │            ├─【有】────────────────→ 走 ③   （最常見，9 成走這條）
  │            ├─【只有截圖／OCR】──────→ 走 ⑤
  │            └─【什麼都沒有】──────────→ 走 ④
  │
  └─【沒有／主旨含 FAILED】→ ④ 去老闆電腦重跑匯出（第 6 節）
                                ├─【跑起來了】────────→ 回到 ①
                                └─【跑不動／老闆在用】→ 走 ⑤

──────────────────────────────────────────────────────────────────────
③ 複製兩行「中間」整段 → 存成 C:\TradeReview\_inbox\YYYY-MM-DD.csv ─────┐
                                                                          │
⑤ 截圖對帳單（要拍全，見 7.1）                                            │
  ▼                                                                       │
⑥ 在 502 這台（放 C:\TradeReview\ 的那台）開 Claude 對話                  │
   貼 PROMPT_轉trades_all.md 整段 ＋ 截圖（第 7 節）                       │
   → AI 更新 trades_all.xlsx，並回報「期初餘額／收盤餘額」                │
  │                                                                       │
  ├─【AI 也不能用】→ ⑦ 逐列手抄成 CSV（第 8 節，最後手段）→ 存 _inbox ─────┤
  ▼                                                                       ▼
★ 雙擊 C:\TradeReview\每日一鍵複盤.bat  ←──────────────────────────────────┘
   ├ 手上有 CSV（③ 或 ⑦）      → 它自己挑檔、自己跑完
   └ 只有 trades_all 更新了（⑥）→ 選單選 2，輸入 交易日／期初／收盤
  │
  ▼
◆ 過程中有出現 🔴 嗎？
  ├─【沒有】→ 開 http://localhost:5500 看複盤 → 貼總結行 → 完成
  └─【有】  → 停手。兩本帳沒有被改到，不要自己修。
               截圖 ＋ _logs\engine_last.txt、writer_last.txt → 交負責人（第 9 節）
```

---

## 3. 情境 A：有草稿、內含 CSV（最常見，佔 9 成）

1. Gmail → **草稿**匣 → 打開主旨 `[SPY-DayTrade-Autosend] YYYY-MM-DD` 那封。
2. 在內文找到這兩行：
   ```
   ===== CSV CONTENT START =====
   ===== CSV CONTENT END =====
   ```
3. **只複製兩行「中間」的內容**（兩行本身不要複製）。
   第一行應該長這樣（或上面多一行 `Account Statement for ...` 也沒關係）：
   ```
   DATE,TIME,TYPE,REF #,DESCRIPTION,Misc Fees,Commissions & Fees,AMOUNT,BALANCE
   ```
4. 開記事本 → 貼上 → **另存新檔**：
   - 位置：`C:\TradeReview\_inbox\`
   - 檔名：**當天日期 + `.csv`**，例如 `2026-07-27.csv`
   - 存檔類型：**所有檔案**（不然會變成 `.csv.txt`）
   - 編碼：**UTF-8**
5. 雙擊 `C:\TradeReview\每日一鍵複盤.bat`，接下來它會自己跑完。中間只會問一次
   「要用這個檔案跑嗎」，確認檔名對就直接等（20 秒沒回答會自動繼續）。

> **數字有逗號（`"75,307.00"`）沒關係**，程式看得懂，不用手動清掉。
> **內容含 `Account Trade History`、`Profits and Losses` 等其他區段也沒關係**，程式只讀 `Cash Balance` 那段。

---

## 4. 情境 B：有草稿，但沒有 CSV 區段

可能是老闆電腦匯出到一半失敗，只送出了截圖／OCR 文字。

- 草稿裡若有**截圖**或一堆用 `|` 隔開的文字列 → 當成「只有截圖」，直接跳到 **第 7 節**。
- 草稿裡什麼有用的東西都沒有 → 當成「沒有草稿」，跳到 **第 6 節**。

---

## 5. 情境 C：主旨是 `[SPY-DayTrade-Autosend] FAILED YYYY-MM-DD`

這是老闆電腦**明白告訴你昨晚採集失敗**。

1. 先看信裡寫的失敗原因（通常會寫是哪一步掛掉），**截圖留存**。
2. 直接進 **第 6 節**：去老闆電腦重跑。
3. 若重跑仍失敗 → 進 **第 7 節**（截圖 → 貼 prompt 給這台的對話）。
4. 兩條都不行 → 停，回報負責人：「今天無法取得對帳單，帳未做」。
   **不要用前一天的檔湊、不要憑印象填。**

---

## 6. 情境 D：沒有草稿 → 去老闆電腦重跑匯出

> ⚠️ **這一節的操作步驟目前沒有任何文件記錄**（原始的 `tos_export.ahk` 觸發程式不在
> `C:\TradeReview\`，推測在老闆電腦上，來源不明——見 `MIGRATION.md` §5.6）。
> **請第一次執行過的人把實際步驟填進來**，不要口耳相傳。

### 【待填】老闆電腦上的實際步驟

| 項目 | 內容 |
|---|---|
| 電腦位置 / 名稱 | ______________________ |
| 登入帳號 | ______________________ |
| 要點的程式或捷徑 | ______________________（原始為 AutoHotkey 腳本 `tos_export.ahk`） |
| 要先開著 thinkorswim 嗎 | ______________________ |
| 跑完會產生什麼 | ______________________（預期：一個 `.txt`，內含 `===== CSV CONTENT =====` 區段） |
| 產物在哪 | ______________________ |
| 大概要跑多久 | ______________________ |

### 跑完之後怎麼確認成功

- **最快的確認**：回到 Gmail，看草稿匣有沒有出現當天的
  `[SPY-DayTrade-Autosend] YYYY-MM-DD` → 有就回到 **第 3 節**。
- 或者：直接把老闆電腦產生的那個 `.txt` **整個複製到隨身碟**帶回來，
  改名成 `2026-07-27.csv` 丟進 `_inbox\`（副檔名一定要是 `.csv`，
  不然一鍵檔掃不到；也可以直接把檔案**拖到 `每日一鍵複盤.bat` 上面放開**，那樣副檔名不拘）。
  整個檔照原樣丟進去就行，**不用先剪掉其他區段**。

### 什麼時候不要硬跑

- 老闆正在用那台電腦交易 → **不要動**，改走 **第 7 節**（截圖交 AI）。
- 你不確定按下去會發生什麼 → **不要按**，問負責人。

---

## 7. 情境 E：只有截圖 → 貼給 502 這台的 Claude 對話（**標準做法**）

> **不要逐列手抄。** 截圖判讀交給 AI，你只負責「拍好截圖」和「檢查它回報的結果」。
> **必須在 502 這台（放 `C:\TradeReview\` 的那台）開對話**——AI 要直接寫 `trades_all.xlsx`，
> 在別台電腦開對話它碰不到檔案。

### 7.1 截圖要拍到什麼（拍不全＝白做）

在 thinkorswim → **Account Statement** → **Cash Balance** 區段，必須拍到：

- ✅ 欄位標題那一行：`DATE / TIME / TYPE / REF # / DESCRIPTION / Misc Fees / Commissions & Fees / AMOUNT / BALANCE`
- ✅ 當天那一行 **`BAL`**：`Cash balance at the start of business day DD.MM CST`（**期初餘額，缺這行做不了**）
- ✅ 當天**所有** `TRD` 列，一列都不能少（分批成交的同一張單也要每列都拍）
- ✅ **`Misc Fees` 欄一定要拍清楚**（手續費只能用實際值，不准估算）
- ✅ **`BALANCE` 欄一定要拍清楚**（這是防呆的命根子）
- ✅ 午夜之後（時間 `00:xx`、`01:xx`）那幾列也要拍到

分多張拍沒關係，但**列與列之間不能有斷層**。

### 7.2 在 502 這台開對話，把 prompt 和截圖一起貼進去

1. 在 **502 這台**（就是放 `C:\TradeReview\` 的那台）開 Claude 的對話視窗。
2. 打開 `C:\TradeReview\PROMPT_轉trades_all.md`，複製
   「⬇⬇ 從這裡開始複製 ⬇⬇」到「⬆⬆ 複製到這裡結束 ⬆⬆」**中間的整段**（框線那兩行不要複製）。
3. 把那整段貼進對話，**同一則訊息裡連同今天的截圖一起送出**（截圖可以多張）。
4. 告訴它今天是哪一天（例如「這是 2026-07-27 早上抓的，美東交易日應該是 07-26」），
   讓它有東西可以對照。**不確定就不要講**，寧可不講也不要講錯。

> 這段 prompt 已經把所有鐵則寫進去了（時區 −12h 歸日、拆單合併、LIFO 配對、
> 手續費取實際值不估算、逐列驗餘額、留倉就停）。**你不用自己記這些，也不要自己改 prompt。**

### 7.3 AI 做完之後，你要檢查它有沒有給你這三樣

| # | 應該拿到 | 沒拿到怎麼辦 |
|---|---|---|
| 1 | 「`trades_all.xlsx` 已更新，新增 N 列」 | 追問；沒寫進檔案就等於沒做 |
| 2 | **期初餘額**（`Cash balance at the start of business day` 那行） | 追問；下一步一定要用 |
| 3 | **收盤餘額**（當天最後一筆的 `BALANCE`） | 追問；**不要省略**，這是頭尾雙重 check 的關鍵 |

**只要 AI 講到下面任何一句，就停手交負責人**（這些是特例日，不在自動範圍）：

- 「有留倉」／「跨日平倉」／「收盤還有未平部位」
- 「餘額對不上」／「第 N 列驗算失敗」
- 「有入金／出金／配息」等非交易的現金異動
- 「合併後不是 100 股」

### 7.4 接第二段：把 CS 帳做完（**這一步永遠不需要 AI**）

最省事：雙擊 `每日一鍵複盤.bat` → `_inbox` 沒有 CSV 時會跳選單 → 選 **`2`** →
依序輸入 AI 給你的**交易日、期初餘額、收盤餘額** → 它會先試算給你看，
看到「試算通過」再讓它寫入。

或者自己下指令（效果一樣）：

```bash
python C:\TradeReview\cs_from_trades.py --date 2026-07-27 --open-bal 37874.49 --close-bal 37668.99
```

看到 `🟢 全部檢核通過` 之後，再加 `--commit` 實際寫入：

```bash
python C:\TradeReview\cs_from_trades.py --date 2026-07-27 --open-bal 37874.49 --close-bal 37668.99 --commit
```

出現 `🔴` → **停手交負責人，不要手動改數字硬湊**。

> 為什麼第二段不需要 AI：買進沒有手續費，所以淨額可以從 trades_all 的價格與損益
> **精確反推**（連每筆 1.54/1.55/1.56 的實際手續費都還原得出來），不是估算。

---

## 8. 最後手段：AI 也不能用 → 逐列手抄 CSV

> **只有在「只有截圖」而且「AI 完全不能用」時才走這條。** 平常請走第 7 節。
> 好消息是：**抄錯不用怕**——引擎會拿對帳單自己的 `BALANCE` 欄逐列重算，
> 任何一列對不上會**當場擋下來**並告訴你是哪一列。

用記事本，第一行固定照抄：

```
DATE,TIME,TYPE,REF #,DESCRIPTION,Misc Fees,Commissions & Fees,AMOUNT,BALANCE
```

然後每一列一行：

```
7/23/26,13:00:00,BAL,,Cash balance at the start of business day 23.07 CST,,,,37896.72
7/23/26,21:53:11,TRD,="1007301234567",SOLD -100 SPY @740.05,-1.54,,74005.00,-36108.28
7/23/26,22:00:09,TRD,="1007301234890",BOT +100 SPY @739.87,,,-73987.00,-110095.28
```

規則：

- 買進（`BOT`）通常**沒有**手續費 → `Misc Fees` 欄**留空**。
- 賣出（`SOLD`）手續費是**負數**，照抄（`-1.54`／`-1.55`／`-1.56` 每天不一定一樣，**不要套固定值**）。
- `Commissions & Fees` 欄一律留空（兩個逗號連著）。
- 數字可以有逗號也可以沒有，程式都看得懂。
- 分批成交（同一個 REF # 出現好幾列，例如 +20/+27/+53）→ **照抄成好幾列**，程式會自動合併，
  **不要自己加總**。
- ⚠️ **跨午夜那幾列**（時間 `00:xx`、`01:xx`…）的 `DATE` 欄要寫**它們真正的日曆日**
  （通常是**隔一天**），**不要照抄券商畫面上的 Trade Date**。抄錯會整天差一天。

抄完存成 `C:\TradeReview\_inbox\YYYY-MM-DD.csv`（UTF-8），雙擊 `每日一鍵複盤.bat`。
格式看不懂就打開範例檔對照：`_inbox6-07-22_23_example.csv`（裡面就有分批成交與跨午夜列）。

---

## 9. 出現 🔴 怎麼辦（最重要的一節）

一鍵檔在**寫入之前**就會擋下來，**兩本帳一個字都不會被改到**。你要做的只有三件事：
**截圖 → 存證 → 交負責人**。

| 畫面上出現 | 意思 | 你要做什麼 |
|---|---|---|
| `BALANCE 對不上 @ …` | 某一列抄錯，或**漏抄了一整列** | 回去看截圖，通常就是它指的那一列附近漏了。補好再跑一次 |
| `留倉：…` / `留倉=True` | 當天收盤還有沒平掉的部位 | **特例日，不在自動範圍**。交負責人 |
| `⚠️需人工確認：…` | 引擎自己也不敢決定 | 交負責人 |
| `Σcol1` 和 `Σpnl` 兩個數字不一樣 | 帳對不起來 | 交負責人 |
| `找不到 Cash Balance 表頭` | CSV 沒抄到對帳單那段，或存檔格式錯 | 重存一次（第 3 節第 4 步），檢查第一行欄位名 |
| `缺少必要檔案` | 資料夾少東西 | 交負責人（見 `搬遷502_更新清單.md`） |
| `偵測到 Excel 鎖定檔` | 有人開著那兩本 Excel | 請他關掉，再跑一次 |

**交件時附上這兩個檔**（一鍵檔已自動幫你存好）：

```
C:\TradeReview\_logs\engine_last.txt
C:\TradeReview\_logs\writer_last.txt
```

### 特殊狀況：舊日期把今天擋住了

匯出檔常常一次含好幾天。如果 🔴 指的是**以前那幾天**（早就記過帳了），
而今天本身是乾淨的，就用「只跑今天」的方式繞過——在 PowerShell 執行：

```bash
python C:\TradeReview\spy_daytrade_engine.py C:\TradeReview\_inbox\2026-07-27.csv --date 2026-07-27
```

看全綠後再寫入：

```bash
python C:\TradeReview\spy_daytrade_writer.py C:\TradeReview\_inbox\2026-07-27.csv --date 2026-07-27 --commit
```

`--date` 填的是**美東交易日**（畫面上 `SESSION 2026-07-27` 顯示的那個日期）。
**如果 🔴 指的是今天，這招不能用，一律交負責人。**

---

## 10. 補跑：昨天（或前幾天）忘了做

1. 找出那幾天的草稿（Gmail 草稿匣往回翻）或截圖。
2. **一天存一個 CSV**：`_inbox\2026-07-25.csv`、`_inbox\2026-07-26.csv`…
3. **按日期由舊到新，一天跑一次**一鍵檔（把檔案拖到 .bat 上放開最省事）。
   **順序不能亂**——CS 帳是一天接一天往下長的。
4. 重複跑同一天**沒有關係**：程式會顯示「冪等跳過」，不會寫兩次。
   所以「不確定剛剛有沒有成功」時，**再跑一次是安全的**。

---

## 11. 做完的收尾檢查（30 秒）

- [ ] 畫面最後有出現 `全部完成`，而且中間沒有任何 🔴
- [ ] 瀏覽器 `http://localhost:5500` 打得開，切到當天看得到 K 線與交易線
- [ ] 貼社群的總結行（格式如下，數字**從畫面上抄，不要自己算**）：
      ```
      07/27(週一) SPY 當沖 800股 損益 -205.50 -0.03%
      ```
      - `N股` ＝ **完整來回筆數 × 100**（一次進場＋一次出場算一筆，不是把買量賣量相加）
      - 損益 ＝ 引擎印的 `Σpnl`
      - 百分比 ＝ 損益 ÷ |買進總額|（`CS交易紀錄.xlsx` 當天區塊的「當日損益 %」就是這個數字）
      - 有留倉／平倉／非 SPY → 各自另起一行，**日期只寫第一行**
- [ ] 看完複盤後把「TradeReview App」那個黑視窗關掉

---

## 附錄 A：今天要用到的東西在哪

| 東西 | 位置 |
|---|---|
| 一鍵執行 | `C:\TradeReview\每日一鍵複盤.bat`（雙擊，或把 CSV 拖上去） |
| 今天的 CSV 要放這 | `C:\TradeReview\_inbox\YYYY-MM-DD.csv` |
| 出事要交的紀錄 | `C:\TradeReview\_logs\engine_last.txt`、`writer_last.txt` |
| Gmail 帳號 | `shuye1668@gmail.com` → **草稿**匣 |
| 草稿主旨 | `[SPY-DayTrade-Autosend] YYYY-MM-DD` |
| 兩本帳 | `trades_all.xlsx`、`CS交易紀錄.xlsx`（**可以看，看完不要存檔**） |
| 怎麼跑（詳細） | `操作手冊.md` |
| 只有截圖時要貼的 prompt | `PROMPT_轉trades_all.md`（在 **502 這台**開對話，連截圖一起貼） |
| 手抄 CSV 的範例檔（最後手段） | `_inbox\2026-07-22_23_example.csv`（含分批成交與跨午夜列） |

## 附錄 B：名詞

- **對帳單（Account Statement）**：券商給的當天現金流水帳，每筆都有金額與當下餘額。
- **BAL 行**：每天的期初餘額。**沒有它就做不了帳。**
- **TRD 行**：一筆成交。只有這種列要抄。
- **CST / EDT**：對帳單時間是台北時間(CST)，程式會自動換成美東(EDT)，你不用自己換。
- **留倉**：當天收盤還留著沒平的部位 → **特例日，交負責人**。
- **冪等**：同一天重複跑只會寫一次，所以重跑是安全的。
- **全綠**：`Σcol1` 和 `Σpnl` 兩個數字一樣、`留倉=False`、而且沒有任何警告。

## 封底：出事找誰

- 系統擁有者 / 負責人：______________（請填）
- 老闆電腦相關（第 6 節）找誰：______________（請填）
- 遇 🔴、數字對不上、特例日 → **截圖 + `_logs\` 兩個檔 + 當天 CSV**，交上面那位，
  **不要自己硬處理**。
