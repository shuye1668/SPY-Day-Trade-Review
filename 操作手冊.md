# SPY 當沖每日複盤系統 — 操作手冊

> 給誰看：**任何同仁**，不需要先懂這套系統。照這份做就能完成每天的處理。
> **這份講「怎麼跑」；「今天資料從哪來、沒草稿怎麼辦」看 → `人工SOP_每日取料與判斷.md`。**
> 看不懂專有名詞 → 跳到最後的「附錄 A：名詞小辭典」。
> 最重要的一條：**看到 🔴 紅旗就停手找負責人（見封底聯絡人），千萬不要自己湊數字。**
> 最後更新：2026-07-24。

---

## 0. 這套系統在做什麼（一分鐘看懂）

老闆每天用 thinkorswim 交易 SPY（美股大盤 ETF），每筆固定 100 股，一天做十幾筆當沖。
這套系統每天做三件事：

1. 把當天券商的「對帳單」自動算成兩本 Excel 帳（交易明細 + 損益）。
2. 自動用對帳單自帶的餘額欄**逐筆驗算**，算錯或漏單會**當場擋下來**（不會默默寫錯）。
3. 開一個網頁，把當天每筆買賣畫在 K 線圖上，方便複盤。

**你的角色很簡單**：把當天對帳單餵給「引擎」，看它是不是「全綠」，全綠就讓它寫入、開網頁；
不綠（出現 🔴）就停下來找人。真正的計算和驗證都是引擎在做，你不需要自己算。

**所有東西都在這個資料夾**：`D:\fileserver_D\TradeReview\`

### 流程分兩段（重要：這樣設計是為了「AI 不能用時，人工照樣能跑」）

```
第一段：對帳單 → trades_all.xlsx（交易明細帳）
        ├─ 有 CSV  → 跑引擎，全自動，不需要 AI       ← 最常見
        └─ 只有截圖 → 請 AI 幫忙判讀（見第 2 節情況 B）
第二段：trades_all.xlsx → CS交易紀錄.xlsx（會計帳）
        └─ 跑 cs_from_trades.py，純程式、永遠不需要 AI
```

第二段完全不依賴 AI：它只讀 trades_all 就能精確算出所有數字（連每筆手續費都能還原）。
所以就算哪天 AI 不能用，只要 trades_all 有了，你照樣能把帳做完。

---

## 1. 每天怎麼跑（快速版）

### ⭐ 最簡單：雙擊 `每日一鍵複盤.bat`（推薦；取代下面 🅰 的三個指令）

把當天對帳單存成 `D:\fileserver_D\TradeReview\_inbox\2026-07-24.csv`（怎麼存見第 2 節步驟 1），
然後**雙擊 `D:\fileserver_D\TradeReview\每日一鍵複盤.bat`**。它會照順序自動做完六步：

```
環境檢查 → 關掉還開著的舊網頁 → 挑出 _inbox 裡最新的 CSV → 引擎乾跑檢查
        → 全綠才寫入兩本帳 → 啟動網頁並自動打開瀏覽器
```

- 中途只問一次「要用這個檔案跑嗎」，**20 秒沒回答就自動繼續**（按 `N` 取消）。
- **任何一關沒過就停下來、不寫入**，畫面上會印 🔴 和該怎麼辦；帳本一個字都不會被改到。
- 也可以把 CSV **直接拖到這個 .bat 上面放開**，就指定用那個檔跑。
- 完整輸出留在 `_logs\engine_last.txt` 和 `_logs\writer_last.txt`，出事直接把這兩個檔給負責人。
- `_inbox` 裡沒有 CSV 時，它會跳一個小選單：只開網頁 ／ 手動跑第二段（要自己輸入期初收盤餘額）／ 離開。
- 它**只是幫你按順序跑第 2、3、4 節那幾個指令**，不會自己算任何數字，判斷標準跟第 4 節完全一樣。

下面的 🅰 🅱 是**手動逐行**的做法。一鍵檔不能用、或只想單獨重跑某一步時才需要。

---

先開終端機：按鍵盤 **Win 鍵** → 打 `powershell` → 按 **Enter**，跳出藍色/黑色視窗就對了。
把下面每一行**整行複製貼上、按 Enter**。把 `2026-07-24` 換成**當天日期**。

### 🅰 有對帳單 CSV（最常見，3 個指令，全程不需要 AI）

```
python D:\fileserver_D\TradeReview\spy_daytrade_engine.py D:\fileserver_D\TradeReview\_inbox\2026-07-24.csv
```
→ 看輸出：最後一行 `Σcol1` 和 `Σpnl` **兩個數字要一樣**、`留倉=False`，而且**下面沒有**「警告/需人工確認」。
　全部符合＝**全綠**，做下一步。有任何 ⚠️ 或紅字＝**停**，跳到第 4 節。

```
python D:\fileserver_D\TradeReview\spy_daytrade_writer.py D:\fileserver_D\TradeReview\_inbox\2026-07-24.csv --commit
```
→ 看到 `✅ N 個交易日寫入完成並通過 §4 核驗` 就成功了（兩本帳一次寫好）。看到 `🔴` 就停，跳到第 4 節。

```
python D:\fileserver_D\TradeReview\trade_review_app.py
```
→ 這個視窗會「卡住不動」是**正常的**（它在當網頁伺服器）。打開瀏覽器輸入網址
　`http://localhost:5500` 就能看當天 K 線圖。看完要關掉時，回到這個視窗按 **Ctrl + C**。

### 🅱 只有截圖（需要 AI 幫忙判讀第一段）

1. 請 AI 把截圖轉成 trades_all：把 `PROMPT_轉trades_all.md` 裡框起來的整段貼給 Claude，
   連同截圖一起給它。AI 會寫好 `trades_all.xlsx`，並**回報當天的期初餘額與收盤餘額**。
2. 用 AI 回報的兩個數字，跑第二段（**這一步是純程式，AI 不能用時你自己跑也一樣**）：
```
python D:\fileserver_D\TradeReview\cs_from_trades.py --date 2026-07-24 --open-bal 37874.49 --close-bal 37668.99
```
→ 先不加 `--commit`，這是「試算」，不會動到檔案。看到 `🟢 全部檢核通過` 才做下一步；
　看到 `🔴` 就停，跳到第 4 節。
3. 確認沒問題，再加 `--commit` 真正寫入：
```
python D:\fileserver_D\TradeReview\cs_from_trades.py --date 2026-07-24 --open-bal 37874.49 --close-bal 37668.99 --commit
```
4. 最後一樣跑 `trade_review_app.py` 開網頁。

> 期初/收盤餘額在對帳單哪裡找？期初 = `Cash balance at the start of business day` 那一行的餘額；
> 收盤 = 當天最後一筆交易的 BALANCE 欄。兩個都給，程式才能做「頭尾雙重check」——
> 這是抓錯的關鍵，**不要省略 `--close-bal`**。

做完了。要貼社群的「總結行」看第 5 節。

---

## 2. 每天怎麼跑（詳細版）

### 步驟 1：拿到當天的對帳單，存成 CSV 檔

對帳單有兩種來源：

**情況 A：有匯出檔或 CSV（最常見、最簡單）**
老闆的電腦會匯出一個 `.txt` 檔，裡面有一段用
`===== CSV CONTENT START =====` 和 `===== CSV CONTENT END =====` 包起來的內容。
把**這兩行中間**的內容整段複製，存成一個純文字檔，副檔名改成 `.csv`，放到
`D:\fileserver_D\TradeReview\_inbox\` 資料夾，檔名用當天日期，例如 `2026-07-24.csv`。

**情況 B：只有截圖（比較麻煩，沒把握就找負責人）**
把截圖的每一列照抄成 CSV。格式與範例見
`D:\fileserver_D\TradeReview\_inbox\2026-07-22_23_example.csv`（直接打開參考）。規則：
- 第一行必須是欄位名：`DATE,TIME,TYPE,REF #,DESCRIPTION,Misc Fees,Commissions & Fees,AMOUNT,BALANCE`
- 每筆交易一行，照截圖數字抄。手續費（Misc Fees）是負數，買進（BOT）通常沒有手續費就留空。
- **陷阱**：截圖裡「午夜之後」（時間 00:xx、01:xx…）那幾列，日期要寫**它們真正的日曆日**
　（通常是隔一天），不要照抄券商顯示的營業日。抄錯會差一天。沒把握 → 找負責人。

> 好消息：就算情況 B 抄錯一個數字，引擎的餘額驗算會**當場抓出來**（第 4 節的「BALANCE 對不上」），
> 不會默默算錯。所以抄完一定要先跑引擎乾跑（步驟 2）。

### 步驟 2：跑「引擎乾跑」檢查（不會動到任何檔案，可以安心跑）

```
python D:\fileserver_D\TradeReview\spy_daytrade_engine.py D:\fileserver_D\TradeReview\_inbox\2026-07-24.csv
```

它會印出當天每一筆交易、方向（多/空）、損益，最後一行是總計。**這一步不寫入任何東西**，
純檢查，跑幾次都沒關係。怎麼判斷全綠 → 第 4 節。

### 步驟 3：全綠才寫入

```
python D:\fileserver_D\TradeReview\spy_daytrade_writer.py D:\fileserver_D\TradeReview\_inbox\2026-07-24.csv --commit
```

這一步才真正寫進兩本 Excel 帳。它**寫入前會自動先備份**、**寫入後自動再驗算一次**、
**如果驗算不過會自動還原、不留半套**。而且**同一天重複跑不會寫兩次**（會顯示「冪等跳過」）。
所以萬一你不確定剛剛有沒有寫成功，再跑一次是安全的——已經寫過的它會自己跳過。

### 步驟 4：開複盤網頁

```
python D:\fileserver_D\TradeReview\trade_review_app.py
```
等它印出啟動訊息後，瀏覽器開 `http://localhost:5500`。左右方向鍵可切換不同交易日。
看完在終端機視窗按 **Ctrl + C** 關閉。

---

## 3. Excel 帳長什麼樣、在哪

寫入會動到兩個檔（都在 `D:\fileserver_D\TradeReview\`）：

- **trades_all.xlsx**：每筆買賣一列，記進出場時間、價格、損益、配對關係。網頁 App 靠它畫圖。
- **CS交易紀錄.xlsx**：會計式帳本，有兩個分頁：
  - `個別識別法`：每天一個區塊，逐筆記現金流、當天餘額、當日損益。
  - `累積損益check`：每天一列，記當日損益與累積損益。

平常**不需要手動打開改它們**。要看數字可以打開來看，但**看完不要存檔**（避免動到公式）。

---

## 4. 怎麼判斷「全綠」還是「🔴 要停」

### ✅ 全綠（可以繼續）
引擎（步驟 2）輸出最後一行像這樣，而且**下面沒有**「警告/需人工確認」那一段：
```
 ── Σcol1=-205.50  Σpnl=-205.50  留倉=False
```
重點：`Σcol1` 和 `Σpnl` **兩個數字一樣**、`留倉=False`。

寫入（步驟 3）成功會印：
```
✅ 2 個交易日寫入完成並通過 §4 核驗
```

### 🔴 要停（不要繼續，找負責人）
只要看到下面任何一種，就是引擎/寫入器在說「這天我不敢自動處理」——
**它已經幫你擋住、什麼都沒寫壞**，你要做的就是**停下來找負責人**，把畫面截圖給他：

- `🔴 以下 session 需人工確認，全部不寫入：`
- `BALANCE 對不上 @ …`（代表某筆數字抄錯或漏單）
- `留倉：…`（當天收盤還有沒平掉的部位，要特別記帳）
- `⚠️需人工確認：…`
- `Σcol1` 和 `Σpnl` 兩個數字**不一樣**

> 這些「特例日」（留倉、跨日平倉、配息、餘額對不上）需要專業判斷，**不在自動範圍內**。
> 引擎擋下來是**正常且正確**的保護，不是壞掉。**千萬不要為了跑完流程去手動改數字硬湊**——
> 這正是以前出過大錯的原因。

---

## 5. 怎麼讀「總結行」（貼社群用）

引擎輸出裡每筆有方向和損益。總結行格式（自己照著填，或請負責人確認）：
```
07/24(週幾) SPY 當沖 N股 損益 ±X.XX ±X.XX%
```
- `N股` = 當天**完整來回筆數 × 100**（一次進場+一次出場算「一筆」；不是把買和賣的量加起來）。
- 損益 = 引擎印的 `Σpnl`。
- 當天若有新留倉、或平掉舊留倉，會多幾行（但那種情況引擎多半會標 🔴，交負責人處理）。

---

## 6. 常見問題排除

| 狀況 | 怎麼辦 |
|---|---|
| 一鍵檔視窗一閃就不見 | 不會發生（它結尾一定會停住等你按鍵）。若真的閃退，改用第 2 節手動指令，並把 `_logs\` 裡的檔給負責人。 |
| 一鍵檔說 `Python 3 not found` | 這台電腦沒裝好 Python。找負責人，或見「附錄 B」。 |
| 一鍵檔挑到錯的 CSV | 它挑 `_inbox` 裡**最新**的 `.csv`（跳過檔名含 example/sample 的）。確認畫面按 `N` 取消，把舊 CSV 移走或直接把正確的檔拖到 .bat 上。 |
| 打指令說 `python 不是內部或外部命令` | 這台電腦沒裝好 Python。找負責人，或見「附錄 B」。 |
| 引擎說 `找不到 Cash Balance 表頭` | CSV 檔內容不對（沒抄到對帳單那段，或格式錯）。重存一次；情況 B 抄的話檢查第一行欄位名。 |
| 出現 `🔴` 或 `BALANCE 對不上` | **正常保護，停手找負責人**（見第 4 節）。不要手動改數字。 |
| 寫入說 Excel 被鎖 / `~$` 檔 | 有人開著那個 Excel。請他關閉，再跑一次步驟 3。 |
| 網頁打不開 / `localhost:5500` 連不上 | 確認步驟 4 那個視窗還開著（沒被 Ctrl+C 關掉）；或換 Chrome 再試。 |
| 說 port 5500 被占用 | 之前的網頁還在跑。把舊的那個終端機視窗按 Ctrl+C 關掉，再跑一次。 |
| 不小心跑了兩次寫入 | 沒關係，第二次會顯示「冪等跳過」，不會重複寫。 |

---

## 附錄 A：名詞小辭典

- **當沖**：當天買、當天賣掉，一來一回。**留倉**：當天收盤還留著沒賣。**平倉**：把之前留倉的部位賣掉。
- **多 / 空**：先買後賣＝做多；先賣後買＝做空（放空）。損益兩種算法一樣，只是順序不同。
- **對帳單 (Account Statement)**：券商給的當天現金流水帳，每筆都有金額和「當下餘額 (BALANCE)」。
- **BALANCE 交叉驗證**：引擎拿「開盤餘額＋每筆金額」自己加一遍，跟對帳單的 BALANCE 欄逐列對。
　對不上＝有筆數字錯或漏了，馬上擋下。這是這套系統最重要的防呆。
- **LIFO 配對**：把買和賣配成一組來算每筆賺賠（後進先出）。引擎自動做，你不用管。
- **offset / Day Header**：帳本每天的「期初餘額」＝券商開盤餘額＋一個固定校正值 offset（目前 = 0，
　存在 `offset_state.json`）。只有留倉/平倉那種特例日 offset 才會變，而那種日子引擎會標 🔴 交人處理。
- **CST / EDT**：對帳單時間是台北時間(CST)，系統會自動換成美東時間(EDT)，你不用自己換。
- **冪等**：同一天重複跑寫入，只會寫一次、不會重複。所以「不確定有沒有成功就再跑一次」是安全的。

## 附錄 B：這台電腦要有什麼（第一次設定，或搬新電腦）

- 要裝 Python，且指令 `python` 能用。裝完套件：`pip install flask pandas numpy openpyxl yfinance`
- 整個 `D:\fileserver_D\TradeReview\` 資料夾複製過去就能用（程式用相對路徑，換路徑不用改）。
- **一定要一起複製 `offset_state.json`**（漏了會導致第一天期初餘額錯）。
- 詳細搬機步驟見 `D:\fileserver_D\TradeReview\MIGRATION.md`。

## 附錄 C：檔案清單（哪個檔是幹嘛的）

| 檔案 | 用途 | 平常要不要動 |
|---|---|---|
| `每日一鍵複盤.bat` | **每天雙擊這個就好**：按順序跑完下面三支程式並開網頁 | 不要改 |
| `spy_daytrade_engine.py` | 引擎：分析對帳單、驗算（路線 🅰 第 1 步） | 不要改 |
| `spy_daytrade_writer.py` | 一步寫兩本帳（路線 🅰 第 2 步，需要 CSV） | 不要改 |
| `cs_from_trades.py` | **從 trades_all 產生 CS 帳**（路線 🅱；純程式、不需 AI） | 不要改 |
| `人工SOP_每日取料與判斷.md` | **取料決策樹**：有無草稿、去老闆電腦、截圖手抄、🔴 怎麼辦 | 每天照著判斷 |
| `PROMPT_轉trades_all.md` | 給 AI 的指令：截圖/草稿 → trades_all（路線 🅱 第 1 步） | 照著複製貼上 |
| `PROMPT_截圖轉CSV_雲端版.md` | 給 AI 的指令：截圖 → **對帳單 CSV**（純轉錄）。沒有這台電腦可用時（臨時借別的 AI 帳號／手機）走這支，之後接路線 🅰 全自動 | 照著複製貼上 |
| `trade_review_app.py` | 複盤網頁 | 不要改 |
| `trades_all.xlsx` / `CS交易紀錄.xlsx` | 兩本帳（系統自動寫） | 可看，別存檔 |
| `offset_state.json` | 期初餘額校正值 | 只有負責人在特例日才改 |
| `_inbox\` | 放每天的 CSV | 你每天存 CSV 到這 |
| `_logs\` | 一鍵檔留下的最近一次完整輸出 | 出事時截這裡的檔 |
| `CLAUDE.md` / `SKILL.md` / `MIGRATION.md` | 技術文件（給負責人/工程） | 不用看 |
| `archive\` | 舊檔封存 | 不用理 |

## 封底：出事找誰

- 系統擁有者 / 負責人：______________（請填）
- 遇 🔴 紅旗、數字對不上、特例日 → 截圖 + 當天 CSV，交給上面這位，**不要自己硬處理**。
