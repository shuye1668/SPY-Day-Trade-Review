# 搬遷到 502 — 更新清單（2026-07-24 ~ 07-27 這批改動）

> 用途：對照著把這台改好的東西搬到 502。打勾表可直接印出來用。
> **最省事的做法**：整個 `D:\fileserver_D\TradeReview\` 覆蓋過去（見 §3），就不會漏。
> 但 §2 那個檔**在資料夾外面**，整包複製也帶不到，一定要另外處理。

---

## 1. 資料夾內：要複製的檔案

目的地一律是 502 的 **`D:\fileserver_D\TradeReview\`**（同名同路徑最保險；不過程式已全改相對路徑，放別的路徑也能跑）。

### 🆕 新增的檔（502 上還不存在）

| ☐ | 檔名 | 這是什麼 |
|---|---|---|
| ☐ | `每日一鍵複盤.bat` | **一鍵執行**：雙擊就跑完引擎檢查→寫入→開網頁。純 ASCII 內容（避免 cmd 多位元組解析問題），中文訊息由 Python 印出 |
| ☐ | `cs_from_trades.py` | **本次核心**。讀 trades_all → 產生 CS交易紀錄。純程式、不需要 AI |
| ☐ | `PROMPT_轉trades_all.md` | 給 AI 的指令：截圖/草稿 → trades_all（只有截圖時才用） |
| ☐ | `PROMPT_截圖轉CSV_雲端版.md` | 給 AI 的指令：截圖 → 對帳單 CSV（純轉錄）。無檔案權限的雲端 AI／臨時帳號用，之後走一鍵檔全自動 |
| ☐ | `操作手冊.md` | 給同仁看的白話操作說明書（怎麼跑） |
| ☐ | `決策樹.png` | 一頁決策樹圖檔（可列印貼牆）；改流程後用 `決策樹_產圖.py` 重生 |
| ☐ | `決策樹_產圖.py` | 產生上面那張圖的腳本（Pillow＋微軟正黑體） |
| ☐ | `人工SOP_每日取料與判斷.md` | 取料決策樹（草稿有/無、老闆電腦、截圖手抄、🔴 處置）。**§6 老闆電腦步驟是空白的，要現場補填** |
| ☐ | `MIGRATION.md` | 搬機技術文件 |
| ☐ | `offset_state.json` | **期初餘額校正值（目前 0）。漏了帳會錯，且不會報錯** |
| ☐ | `搬遷502_更新清單.md` | 本檔 |
| ☐ | `_inbox\`（資料夾） | 每天放對帳單 CSV 的地方，內含一份範例 CSV |

### ♻️ 有更新的檔（502 上是舊版，要覆蓋）

| ☐ | 檔名 | 改了什麼 |
|---|---|---|
| ☐ | `trade_review_app.py` | 路徑改成相對（資料夾改名/搬家不再壞） |
| ☐ | `spy_daytrade_engine.py` | 輸出改 UTF-8（避免中文亂碼中斷） |
| ☐ | `spy_daytrade_writer.py` | 接上 `--commit` 寫入層＋冪等＋相對路徑 |
| ☐ | `CLAUDE.md` | 加入兩段式架構的權威說明 |
| ☐ | `SKILL.md` | 每日流程改為引擎化 |

### 📊 資料檔（**以 502 現況為準，不要無腦覆蓋**）

| ☐ | 檔名 | 注意 |
|---|---|---|
| ☐ | `trades_all.xlsx` | 已含到 07-23。**若 502 已自行記到更新的日期，不要用這份蓋掉** |
| ☐ | `CS交易紀錄.xlsx` | 同上。兩本帳要一起、且是同一時間點的版本 |
| ☐ | `history_minute.xlsx` | K線快取（4MB），缺了會自動用 yfinance 補，可不搬 |
| ☐ | `colors.xlsx` / `dividends.xlsx` / `events.xlsx` | 設定/配息資料，照搬 |
| ☐ | `notes\`（資料夾） | 每日盤勢筆記 |

> ⚠️ **兩本帳的搬移原則**：`trades_all.xlsx` 與 `CS交易紀錄.xlsx` 必須**成對、同版本**。
> 若兩台都各自記過帳，**先確認哪一台是最新**，不要各搬一半，會對不起來。

---

## 2. 【最容易漏】資料夾外面的檔案

整包複製 `D:\fileserver_D\TradeReview` **帶不到**這個，一定要另外複製：

| ☐ | 來源（這台） | 目的地（502） |
|---|---|---|
| ☐ | `C:\Users\USER\.claude\scheduled-tasks\spy-daytrade-auto-csv\SKILL.md` | `C:\Users\<502帳號>\.claude\scheduled-tasks\spy-daytrade-auto-csv\SKILL.md` |

這是每日 06:47 自動排程的指令內容（已改寫成引擎版，96 行）。
502 的帳號名可能不叫 `USER`，用 `%USERPROFILE%` 找自己的家目錄。

**另外**（要讓 502 上的 AI 判斷與這台一致，選搬）：
- `C:\Users\USER\.claude\CLAUDE.md`（全域守則）
- `C:\Users\USER\.claude\rules\`（判斷規則與踩坑紀錄）
- `C:\Users\USER\.claude\projects\C--\memory\`（12 個記憶檔：offset 模型、總結行格式等）

---

## 3. 複製指令（擇一）

**整包複製（推薦，最不會漏）** — 跳過封存與暫存檔：
```
robocopy "D:\fileserver_D\TradeReview" "D:\TradeReview" /E /XD archive __pycache__ blank_out /XF ~$*
```
把 `D:\TradeReview` 換成隨身碟或 502 的實際路徑（如 `\\502\C$\TradeReview`）。
`/XD archive` 會跳過 5MB 的舊檔封存；想一起帶就拿掉這段。

**只搬程式（資料檔 502 已是最新時用）**：
```
robocopy "D:\fileserver_D\TradeReview" "D:\TradeReview" 每日一鍵複盤.bat 人工SOP_每日取料與判斷.md PROMPT_截圖轉CSV_雲端版.md 決策樹.png 決策樹_產圖.py cs_from_trades.py spy_daytrade_engine.py spy_daytrade_writer.py trade_review_app.py PROMPT_轉trades_all.md 操作手冊.md MIGRATION.md CLAUDE.md SKILL.md offset_state.json 搬遷502_更新清單.md
```

---

## 4. 搬完在 502 上驗證（3 步，約 1 分鐘）

**① 環境**（第一次才需要）
```
pip install flask pandas numpy openpyxl yfinance
```

**② 冒煙測試**——應印出 07-22/07-23 分析、`Σcol1` 與 `Σpnl` 相同、無 🔴：
```
python D:\fileserver_D\TradeReview\spy_daytrade_engine.py D:\fileserver_D\TradeReview\_inbox\2026-07-22_23_example.csv
```

**③ 新腳本＋App**——應印 `🟢 全部檢核通過`（這是試算，不會動檔案）：
```
python D:\fileserver_D\TradeReview\cs_from_trades.py --date 2026-07-23 --open-bal 37896.72 --close-bal 38042.80
```
```
python D:\fileserver_D\TradeReview\trade_review_app.py
```
→ 瀏覽器開 `http://localhost:5500`，切到 07-23 應看到 K 線與 8 組交易。看完按 Ctrl+C。

**④ 一鍵檔**——雙擊 `每日一鍵複盤.bat`。`_inbox` 沒有新 CSV 時會跳選單，選 `1`（只開網頁）
即可確認環境檢查、關舊 App、啟動網頁三段都正常。畫面中文沒亂碼 = 編碼正常。

四步都過 = 搬遷成功。

---

## 5. 不要複製的東西

| 項目 | 原因 |
|---|---|
| `C:\Users\USER\.claude\.credentials.json` | Claude 登入憑證，502 要自己重新登入 |
| `.claude\settings.local.json` | 機器綁定的權限設定 |
| `.claude\projects\` 除 `C--\memory\` 外 | 龐大的對話記錄，沒有用 |
| `.claude\cache\ sessions\ tasks\` 等 | 快取，會自動重生 |
| `archive\dead_pipeline\` | 已廢棄的舊自動管線，**內含舊 Gmail OAuth 憑證**，不想把憑證帶到新機就別複製 |
| `__pycache__\` | Python 暫存，自動重生 |
| `_logs\` | 一鍵檔的最近一次輸出，會自動重生 |

---

## 6. 這批到底改了什麼（背景說明）

1. **資料夾整理**：52 個舊檔（過期備份、被淘汰的每日腳本、暫存檔）移到 `archive\`；
   已死 70 天的自動管線資料夾也一併封存。**都是移動不是刪除**。
2. **每日流程引擎化**：原本每天要靠 AI 手工推算 LIFO/損益/offset，現在改成跑腳本。
   引擎的 `--commit` 寫入層本次補齊，並用 07-22/07-23 做**逐格回歸驗證**（0 差異）。
3. **兩段式架構**（本次重點，為了「AI 不能用時人工照樣能跑」）：
   ```
   第一段 對帳單 → trades_all.xlsx   （有 CSV 全自動；只有截圖才需 AI）
   第二段 trades_all → CS交易紀錄     （cs_from_trades.py，純程式，永不需要 AI）
   ```
   第二段能成立的關鍵：淨額可從 trades_all **精確反推**——買進無手續費故
   `net = −(價×股數)`，賣出 `net = 損益 − 買進net`，能逐筆還原真實手續費
   （1.54/1.55/1.56 各不同），**不是估算**，符合「禁止估算手續費」鐵則。
4. **相對路徑化**：四支程式全部改成以腳本所在資料夾為基準，搬家/改名不再壞。

### 驗證紀錄（都是實跑過的）
- `cs_from_trades.py` 產出與人工核對過的 CS 帳**逐格相同（0 差異）**
- 冪等：同一天重跑不會重複寫
- 負向測試：期初餘額故意打錯 100 元 → 頭尾雙錨點抓出並**拒寫**
- App：換到含中文的新路徑、從別的工作目錄啟動，皆正常讀到資料

### 出問題時的退路
原始檔備份在 `C:\Users\USER\.claude\backups\manual\20260724\`：
- `CS交易紀錄.xlsx`、`trades_all.xlsx`（改動前狀態）
- `SKILL_spy-daytrade-auto-csv_ORIGINAL.md`（原始 1186 行手工版 routine）
