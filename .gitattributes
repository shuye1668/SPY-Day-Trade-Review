# 兩台都是 Windows，但 autocrlf 設定可能不同。明確釘死行為，避免
# (a) xlsx（本質是 zip）被當文字做 CRLF 轉換而損毀，
# (b) 兩台 checkout 出不同行尾造成整檔假 diff。

*                text=auto

# 二進位：絕不可做任何行尾轉換
*.xlsx           binary
*.xlsb           binary
*.xls            binary
*.png            binary
*.jpg            binary
*.pdf            binary

# 對帳單 CSV：不做任何行尾轉換。
# 兩台都是 Windows，統一 LF 沒有實益；反而因為 checkout 會把工作區轉回
# CRLF，index 與工作區永遠對不上，讓每次 git pull 都以
# "You have unstaged changes" 失敗（2026-08-10 Boss PC 就卡在這）。
# 其中 2026-07-29-raw.csv 每行是雙 CR（CR CR LF），git 直接判定為 binary、
# 完全不正規化。改為 -text：index 與工作區逐位元組相同，永遠不會假 dirty。
# 引擎用 csv 模組解析，CRLF/LF 都吃，不受影響。
*.csv            -text

# xlsx 是 binary，git diff 只會顯示 "Bin"。CS 帳本的文字側寫
# （CS交易紀錄_dump.csv）才是可讀 diff 的來源。
CS交易紀錄.xlsx   binary diff=none
