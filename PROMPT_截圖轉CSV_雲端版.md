# AI Prompt — 截圖 → 對帳單 CSV（**純轉錄**，給沒有檔案權限的雲端 AI 用）

> **什麼時候用這支**：手邊只有截圖，而且**沒辦法在 502 那台**（放 `D:\fileserver_D\TradeReview\` 的電腦）
> 開 Claude 對話——例如臨時借用別的 AI 帳號、在別台電腦、只有手機。
>
> **跟 `PROMPT_轉trades_all.md` 的差別**：那支要求 AI **直接寫入 `trades_all.xlsx`**，
> 雲端聊天沒有檔案權限，做不到。這支**只要 AI 做一件事：把截圖上的每一列原封不動打成 CSV 文字**。
> 之後所有計算（時區換算、拆單合併、LIFO 配對、損益、手續費、餘額驗算）
> 一律交給 `每日一鍵複盤.bat` 裡的引擎——那是確定性程式，比讓 AI 算可靠得多。
>
> ⚠️ **上傳前先遮掉帳號**：截圖標題那行含券商帳號（`Account Statement for xxxxxxxxSCHW`），
> 裁掉或塗掉再上傳。這是把財務資料送到外部服務，用完記得刪除對話。

---

⬇⬇ 從這裡開始複製 ⬇⬇

你是資料轉錄員。我會給你券商對帳單（thinkorswim Account Statement 的 **Cash Balance** 區段）的截圖。
請把截圖上的每一列**原封不動**打成 CSV 文字。

## 你只做轉錄，不做任何計算

**嚴禁**做以下任何一件事——這些後面有專門的程式處理，你插手只會製造錯誤：

- ❌ 不要換算時區、不要判斷是哪個美東交易日
- ❌ 不要合併拆單（同一個 REF # 出現好幾列就照抄好幾列）
- ❌ 不要配對買賣、不要算損益、不要算勝率
- ❌ 不要推算或補上手續費（看不清楚就照下面「看不清楚怎麼辦」處理）
- ❌ 不要重新排序、不要刪掉你覺得重複的列
- ❌ 不要「順便修正」任何你覺得怪的數字

## 輸出格式（照這個規格，一個字都不要多）

第一行固定是這一行，原樣照打：

```
DATE,TIME,TYPE,REF #,DESCRIPTION,Misc Fees,Commissions & Fees,AMOUNT,BALANCE
```

接著每一列一行，**9 個欄位、8 個逗號**，依螢幕上由上到下的順序：

```
7/23/26,13:00:00,BAL,,Cash balance at the start of business day 23.07 CST,,,,37896.72
7/23/26,21:53:11,TRD,="1007301234567",SOLD -100 SPY @740.05,-1.54,,74005.00,-36108.28
7/23/26,22:00:09,TRD,="1007301234890",BOT +100 SPY @739.87,,,-73987.00,-110095.28
```

規則：

1. **日期照抄螢幕上 DATE 欄的樣子**，維持 `m/d/yy`（例如 `7/23/26`）。
   **不要**改寫成 `2026-07-23` 之類的格式——程式只認 `m/d/yy`。
2. **時間**照抄 `HH:MM:SS`。
3. **`BAL` 那一列一定要有**（`Cash balance at the start of business day ...`）。
   它是當天的期初餘額，沒有它整天做不了帳。截圖裡若沒有，明講「截圖裡沒有 BAL 列」。
4. **`REF #`** 有沒有 `="..."` 都可以，照抄最省事。
5. **`Misc Fees`**：賣出通常是負數（`-1.54`、`-1.55`、`-1.56` **每天不一樣，照抄**）；
   買進通常是空白或 `--` → 這一欄**留空**（兩個逗號連著）。
6. **`Commissions & Fees`** 這一欄一律**留空**。
7. **數字不要加千分位逗號**：寫 `74003.46`，不要寫 `74,003.46`。
   （若你一定要保留逗號，那整個值要用雙引號包起來：`"74,003.46"`。兩者擇一，不可以只有逗號沒有引號。）
8. **括號代表負數**：螢幕上的 `(1.54)` 要寫成 `-1.54`。
9. `DESCRIPTION` 若在螢幕上被折成兩行，接回成一行再寫。

## 跨午夜那幾列（唯一需要你判斷的地方）

如果時間欄從 `23:xx` 跳到 `00:xx`，代表**過了午夜**。
那之後的列，`DATE` 要用**次一天**（例如 `7/23/26` 之後變成 `7/24/26`）。

若螢幕上那幾列的 DATE 仍顯示前一天（有些畫面顯示的是「營業日」不是日曆日），
**請你改成次一天**，並在最後的檢查區明講你改了哪幾列。
**不確定就明講不確定，不要猜。**

## 看不清楚怎麼辦

任何一格看不清楚、被遮住、或截圖之間有斷層：
**不要猜、不要用前後列推算**。直接在最後的檢查區寫出來：
「第 N 列的 BALANCE 看不清楚」。少一格總比錯一格好——錯的會流進財務報表。

## 最後請附上這個檢查區（放在 CSV **後面**，不要混進 CSV 裡）

```
【檢查區】
BAL 列：有 / 沒有
TRD 列數：__ 列
第一列 BALANCE：__________
最後一列 BALANCE：__________
跨午夜調整：無 / 我把第 __ 列之後的 DATE 改成次一天
看不清楚的地方：無 / __________
```

⬆⬆ 複製到這裡結束 ⬆⬆

---

## 拿到結果之後，操作者要做的事

1. 把 AI 給的 CSV（**只複製 CSV 那一段，檢查區不要**）貼進記事本，
   存成 `D:\fileserver_D\TradeReview\_inbox\YYYY-MM-DD.csv`（UTF-8、存檔類型選「所有檔案」）。
   > AI 若把 CSV 包在 ```` ```csv ```` 區塊裡、或前後多寫了幾句話，**都沒關係**，
   > 引擎會自己從 `DATE,TIME,TYPE,REF` 那一行開始讀。但**中間不可以夾雜說明文字**。
2. 雙擊 `每日一鍵複盤.bat`。
3. **【一定要做】對兩個數字**——這是這條路唯一防「漏抄最後幾列」的關卡：

   | 對什麼 | 怎麼對 |
   |---|---|
   | 引擎印的 `收盤=` | 要等於**截圖最後一列的 BALANCE**（也等於檢查區那個數字） |
   | 引擎印的 `開盤=` | 要等於 `BAL` 那列的餘額 |

   **對不上就停手交負責人。**

> **為什麼特別要對「收盤」**：實測過，如果漏抄的是**最後幾列**、而且那幾列剛好是完整的
> 一買一賣，引擎會全綠放行、完全不報錯（因為剩下的列自己算得通）。
> 中間漏抄、中間插字、缺 BAL 列、千分位沒加引號、日期格式寫錯——這些引擎都會擋下來，
> **只有「尾巴漏抄」擋不住**，所以要靠人眼對這個數字。

---

## 這條路 vs. 在 502 上用 `PROMPT_轉trades_all.md`

| | 雲端純轉錄（本檔） | 502 上跑 `PROMPT_轉trades_all.md` |
|---|---|---|
| AI 要有檔案權限嗎 | **不用**，任何聊天視窗都行 | 要，必須能寫 `trades_all.xlsx` |
| AI 負責什麼 | 只有 OCR 轉錄 | OCR ＋ 時區 ＋ 拆單 ＋ LIFO ＋ 損益 |
| 誰在驗算 | 引擎**逐列**用對帳單 BALANCE 重建 | `cs_from_trades.py` 只有頭尾雙錨點 |
| 攔錯能力 | **強**（每一列都對得上才過） | 較弱（中間互相抵銷的錯抓不到） |
| 之後怎麼走 | 雙擊一鍵檔，全自動 | 還要手動輸入期初／收盤餘額 |

**只要拿得到截圖，優先走這條**——即使 502 那台可用，這條路的攔錯能力還比較強。
