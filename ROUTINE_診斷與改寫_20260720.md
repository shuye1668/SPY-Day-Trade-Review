# Spy daytrade auto csv — 診斷與引擎改寫報告

日期：2026-07-20　範圍：診斷 root causes、把每日拋棄式腳本改寫成單一引擎、對樣本乾跑驗證（未寫入 live 檔）

---

## 一、根因：不是偶發手誤，是架構問題

對帳單 CSV 本身就是**完整、可解析、且每列自帶 BALANCE 校驗**的資料源（欄位含 `REF #` 拆單合併、`Misc Fees` 實際費用、`AMOUNT`、`BALANCE`，BAL 列標每日權威開盤餘額）。但 7 支每日腳本（`process_0520`~`0626`、`write_616`、`fix_and_rerun`）**完全沒讀它**，而是把所有數字手抄成 Python literal 再配寫死的 `assert`。7/16 的漏單、費用、3 萬落差全是「手抄層」漏進來、而 CSV 的 BALANCE 欄本可當場抓出。

每一條都違反 CLAUDE.md 自己訂的鐵則：

| 鐵則 | 要求 | 實際 |
|---|---|---|
| §2 | **嚴禁 `ws.max_row`**，必須值掃描 `last_nonempty_row` | 7 支腳本**全部**用 `ws.max_row`，無一支值掃描 → 區塊位移/空行數不對 |
| §3 | 寫入前**先備份** | 腳本內**無一支**備份 |
| §5 | 檢查 `~$` 鎖定檔 | **無一支**檢查 → 7/16「Excel 鎖住存檔失敗」 |
| §1 | 費用取 `Misc Fees` 實欄、按 REF# 合併拆單 | 沒讀 CSV，全靠手算抄進 literal |
| §4 | 7 項核驗全過、失敗還原 | 只做 offset 校驗；缺空行重掃、B 公式比對、Date 型別檢查、restore |
| — | CLAUDE.md 引「詳見 SKILL.md §0」 | **SKILL.md 根本不存在**（dangling reference，本次已補建） |

**最關鍵缺口**：對帳單 BALANCE 欄的逐筆交叉驗證從未被使用。這一個閘門就能即時攔下 7/16 那類漏單。

---

## 二、改寫：單一引擎 `spy_daytrade_engine.py`

以對帳單 CSV 為唯一真相源，取代所有 `process_XXXX.py`/`write_XXXX.py`：

1. **解析** Cash Balance 區段（自動偵測區段邊界，遇下一張表停止）。
2. **CST→EDT 換算**用 `zoneinfo`（`Asia/Taipei`→`America/New_York`），自動處理夏令(-12h)/冬令(-13h)，不再手算。
3. **按 REF# 合併拆單**：股數加權均價、`Misc Fees`/`AMOUNT` 加總；非 100 股自動標 `⚠️需人工確認`。
4. **費用取實欄**：`col1 = AMOUNT + Misc Fees`（皆取自對帳單）。
5. **依時間 LIFO 配對**（複刻 `trade_review_app._analyse_all_trades` 的當日 LIFO 慣例）→ 產出 `多/空/平`、`B/S`、`Pair_Time`、每筆損益(含費)。
6. **BALANCE 交叉驗證**：逐筆用 `開盤 + Σnet_cash` 重建餘額比對對帳單 BALANCE，任一不符即停（漏單/多單/費用錯的即時閘門）。
7. **§4 全部核驗** + **§0 攔截**：留倉/跨日平倉/開盤餘額不明/BALANCE 對不上 → 填 `⚠️需人工確認`、abort、絕不靜默估算。

預設 **DRY-RUN**，不碰任何檔案；`--commit` 才寫入（寫入層待接，見第四節）。

---

## 三、乾跑驗證結果（樣本 6/14–6/18，未寫 live 檔）

| 情境 | 結果 |
|---|---|
| 純當日進出（6/17、6/18） | **全自動**，`Σcol1 == Σpnl == 收−開` 完全自洽 |
| 對照歷史已定稿資料 | 引擎**比歷史更準**：揪出 3 處舊手抄 1 分錢誤差、2 處時間戳打錯 1 秒（引擎值可用對帳單 BALANCE 反證為正確） |
| 留倉/跨日（6/15、6/16） | 每一筆留倉都標 `⚠️需人工確認`，**不靜默計算**（符合 §0） |
| OCR 漏單負向測試 | 刪掉一筆下午交易 → 下一列立即 `BALANCE 對不上` 並中止（正是 7/16 失效模式，舊流程無此閘門） |

具體佐證：6/17 10:00 賣單為 40+60 拆單，引擎淨額 74974.45（對帳單 BALANCE 36,765.55 反證正確），舊手抄為 74974.44、損益 -137.74 皆少 1 分；引擎 -137.73 為正解。

---

## 四、目前狀態與下一步

**本次已完成（core 已驗證）**：解析、合併、配對、每筆損益、BALANCE 交叉驗證、§0 攔截、§4 核驗邏輯，全部對樣本乾跑通過。

**尚未接上（刻意留待你確認後再做，因會寫 live 財務檔）**：
- `--commit` 實際寫入層：值掃描 `last_nonempty_row` 定位、寫前備份、`~$` 鎖檔檢查、寫後 §4 全核驗、失敗自動還原。程式已把介面與安全需求標好，建議**先對 live 檔的複本測試**再上線。

建議接續：① 把寫入層接上並對 live 檔「複本」測；② 用引擎重跑 7/16（驗證能否重現/修正當時的漏單與 3 萬疑雲）；③ 逐步淘汰 `process_*.py` 舊腳本。
